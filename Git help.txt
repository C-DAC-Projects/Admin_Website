# 1. Fetch latest changes from remote (does NOT merge)
git fetch origin

# 2. Switch to main (so we can base our new branch on latest main)
git checkout main

# 3. Pull the latest main branch from remote and update local main
git pull origin main

# 4. Create a new feature branch from updated main
git checkout -b <new-branch-name>

# 5. Do your work → change files, stage & commit
git add .
git commit -m "Your meaningful commit message"

# 6. Push the new branch to remote and set upstream
git push -u origin <new-branch-name>